# permissions for end users to view searchrulerqueryconnectors.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: search-ruler
    app.kubernetes.io/managed-by: kustomize
  name: searchrulerqueryconnector-viewer-role
rules:
- apiGroups:
  - searchruler.prosimcorp.com
  resources:
  - searchrulerqueryconnectors
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - searchruler.prosimcorp.com
  resources:
  - searchrulerqueryconnectors/status
  verbs:
  - get
