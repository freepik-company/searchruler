# permissions for end users to view searchruleractions.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: search-ruler
    app.kubernetes.io/managed-by: kustomize
  name: searchruleraction-viewer-role
rules:
- apiGroups:
  - searchruler.prosimcorp.com
  resources:
  - searchruleractions
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - searchruler.prosimcorp.com
  resources:
  - searchruleractions/status
  verbs:
  - get
